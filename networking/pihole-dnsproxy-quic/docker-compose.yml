---
services:
  pihole:
    image: pihole/pihole:latest
    container_name: pihole
    restart: unless-stopped
    ports:
      - "53:53/tcp"
      - "53:53/udp"
      - "${PIHOLE_WEB_PORT:-80}:80/tcp"
    environment:
      TZ: ${TZ}
      FTLCONF_webserver_api_password: ${FTLCONF_webserver_api_password}
      FTLCONF_dns_upstreams: dnsproxy#5053
      FTLCONF_dns_listeningMode: all
      FTLCONF_dns_revServers: ${FTLCONF_dns_revServers}
      TEMPERATUREUNIT: ${TEMPERATUREUNIT:-f}
    volumes:
      - etc-pihole:/etc/pihole
      - etc-dnsmasq.d:/etc/dnsmasq.d
    depends_on:
      - dnsproxy
    healthcheck:
      test: [ "CMD", "dig", "@127.0.0.1", "pi.hole", "+short" ]
      interval: 30s
      timeout: 10s
      retries: 3
    # Uncomment if using pihole DHCP
    # cap_add:
    #   - NET_ADMIN
    # Uncomment if using a reverse proxy
    # networks:
    #   - proxy


  dnsproxy:
    image: adguard/dnsproxy:latest
    container_name: dnsproxy
    restart: unless-stopped
    command: >
      --listen=0.0.0.0 --port=5053 --https-port=0 --upstream=quic://unfiltered.adguard-dns.com --bootstrap=9.9.9.9

volumes:
  etc-pihole:
  etc-dnsmasq.d:

    # Uncomment if using a reverse proxy (shared external network)
    # networks:
    #   proxy:
    #     external: true
